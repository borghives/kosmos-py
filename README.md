# kosmos-py

**Kosmos-Py** is a Python-based framework and data persistence layer built around MongoDB. The goal is to have a expected consistent environment for all my services.  It wraps the official MongoDB PyMongo driver (`pymongo`).
